package mypackage;
syntax = "proto3";

message MyMessage {

  string key = 1;
  int32 value = 2;
  repeated NetworkCall network = 3;
};

message NetworkCall {
  bool enabled = 1;
  EnumMessage status = 2;
}

enum EnumMessage {
  success = 0;
  failure = 1;
  loading = 2;
}
